extends ../default/layout
block content
    .login-page
        .container
            .row(style="margin-bottom:0")
                form.col.s12.m8.l6.card-panel.center-align.login-form.offset-l3(action="/login",method="post")
                    h2.truncate Login
                    .row
                        .input-field.col.s12
                            input.validate#email(type="email" name="email")
                            label(for="email") Email
                    .row
                        .input-field.col.s12
                            input.validate#password(type="password" name="password")
                            label(for="password") Password
    
                    button(type="submit").waves-effect.waves-light.btn Login
                    small
                        h6
                            a.account(href="/register") Don't have an account?
